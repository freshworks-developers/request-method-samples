{
  "scripts": {
    "test": "vitest run --coverage",
    "fdk-unit-test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "~2.1.8",
    "vite": "~5.0.0",
    "vitest": "~2.1.8",
    "jsdom": "~26.1.0"
  }
}
